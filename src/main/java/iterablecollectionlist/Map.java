package iterablecollectionlist;

public interface Map {

//    Map<City, List<Person>> map1 = new HasMap<>();
//    Map<City, List<Person>> map2 = new HashMap<>();
//
//    map2.forEach(
//            (key, value) ->
//                map1.merge(
//                        key, value,
//                        (existingPeople, newPeople) -> {
//                            existingPeople.addAll(newPeople);
//                            return existingPeople;
//                        }
//                )
//            );

}
